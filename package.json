{
  "name": "github-hey-run",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "logs": "wrangler tail",
    "check:syntax": "node --check src/config.js && node --check src/index.js && node --check scripts/validate-config.mjs",
    "check:config": "node scripts/validate-config.mjs",
    "check:deploy": "wrangler deploy --dry-run --outdir=dist",
    "check": "npm run check:syntax && npm run check:config && npm run check:deploy"
  },
  "devDependencies": {
    "wrangler": "4.112.0"
  }
}
